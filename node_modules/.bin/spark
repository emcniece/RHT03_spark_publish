#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../spark-cli/bin/spark.js" "$@"
  ret=$?
else 
  node  "$basedir/../spark-cli/bin/spark.js" "$@"
  ret=$?
fi
exit $ret
